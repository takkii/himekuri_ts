#!/usr/bin/env ruby
# frozen_string_literal: true

# vim: filetype=ruby

lib = File.expand_path('lib', __dir__)
$LOAD_PATH.unshift(lib) unless $LOAD_PATH.include?(lib)

require 'himekurits'

print HimekuriTsBasic.running
print ' | ' + HimekuriTsBasicWeb.new.node_running_web_locale
HimekuriTsClass.version